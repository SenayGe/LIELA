import 'package:flutter/material.dart';

class Tour extends StatelessWidget {
  final TabController tabController;
  const Tour({ required this.tabController, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
